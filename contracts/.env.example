# RPC + explorer
ARBITRUM_SEPOLIA_RPC=https://sepolia-rollup.arbitrum.io/rpc
ARBITRUM_ONE_RPC=https://arb1.arbitrum.io/rpc
ARBISCAN_API_KEY=

# Deployer (NEVER commit a real key — use a throwaway for testnet)
DEPLOYER_PRIVATE_KEY=

# Initial on-chain wiring (filled after partial deploys)
# Chainlink ETH/USD feed on Arbitrum Sepolia (confirmed before deploy, see scripts/Deploy.s.sol)
CHAINLINK_ETH_USD_FEED=
# Underlying collateral token address (confidential WETH via Nox, resolved after Nox setup)
COLLATERAL_TOKEN=
# Underlying debt token address (confidential USDC via Nox, resolved after Nox setup)
DEBT_TOKEN=

# For the oracle push helper script
ORACLE_ADDRESS=
VAULT_ADDRESS=
